# ===== AerisQ Backend Layers (GEE Deployment) =====

# Core
fastapi>=0.115.0  # Upgrade for Python 3.13 support
uvicorn[standard]>=0.30.0
pydantic>=2.9.0   # Upgrade for Python 3.13 support
python-multipart>=0.0.9 # Required for OAuth2 forms

# HTTP & Auth
httpx==0.26.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Data Processing
numpy==1.26.3
python-dotenv==1.0.0

# Google Earth Engine (CRITICAL)
earthengine-api==1.7.12

# REMOVED due to Render Free Tier Limits / Compilation Errors:
# - scipy (Fortran missing)
# - rasterio (GDAL missing)
# - geopandas (GDAL missing)
# - psycopg2 (No database)
